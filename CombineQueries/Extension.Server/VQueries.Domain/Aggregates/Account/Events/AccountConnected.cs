using MediatR;

namespace CombineQueries.Domain.Aggregates.Account.Events;

// Кросс-агрегатная связь: аккаунт подключился - кто-то должен обеспечить Translator под его алфавит.
//
// Аргументов НЕТ намеренно. Алфавит, базовый адрес и режимы уже стоят в контексте сервера (его
// выставляет connect до подключения аккаунта), поэтому обработчику незачем получать их конвертом -
// он берёт их оттуда же, откуда потом читает ответ и сам connect.
//
// Событие синхронное: SaveEntitiesAsync диспатчит его и ДОЖИДАЕТСЯ обработчика, поэтому поднявший
// продолжает работу уже с прогретым словарём.
public record AccountConnected : INotification;
