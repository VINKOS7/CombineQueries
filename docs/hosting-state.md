# Состояние хостинга на 13.09.2026, 03:00

Записано, чтобы завтра не искать заново.

## Почему шаги едут 36 секунд вместо 5

**База на хосте пустая.** Не код, не логика кнопок, не мои правки.

- локально в `vqueries`: 1096 строк `VirtualFragments`, 134 `Chains`, 0 `Hypers`;
- на fly в `cq-db`: словарь пустой, цепочек нет.

Из-за этого `products/N` не накрывается ни одним фрагментом и диктуется рунами: шесть кусков `/c/` плюс хвост `/t/`. Семь загрузок по пять секунд кулдауна SDK = ~35 с. В деве тот же адрес стоит два запроса, потому что словарь накрывает `dummyjson.com/products/` одним куском.

В логе клиента это видно по строкам `connect: jumps from db 0` и `request: 2/c … 7/c`.

Дев-посевы из миграций (`DevChainSeed`, `DemoChainSeed`, `DemoCartsSeed`) на хост не попали: они стоят под проверкой `ASPNETCORE_ENVIRONMENT == Development`, а на fly окружение Production.

**Чинится переносом данных**, код менять не надо: скопировать `Translators`, `VirtualFragments` и `Chains` из локального постгреса в `cq-db`.

## Что уже сделано

**Хост.** Приложение `vq` в организации друга `aliaksei-konyshau`, регион `arn`, одна машина.
Адрес `https://vq.fly.dev`.

Секреты на машине: `ConnectionStrings__Context`, `DATABASE_URL`, `Auth__Codeword` (сейчас пустой).

Строка подключения обязана содержать `SSL Mode=Disable` — fly-постгрес по flycast TLS не умеет, без этого сервер поднимается без персиста (`migrations skipped`).

**Машина строго одна.** Протокол держит состояние в памяти процесса: буфер кодового слова, поток сборки, хайперы в ОЗУ. При двух машинах балансировщик рвёт любой многозапросный сценарий. Деплоить только так:

```
fly deploy -c fly.prod.toml --ha=false
```

**Клиент.** Контур переключается дефайнами `CQ_PROD;CQ_FLY` — пункт меню `Tools > CombineQueries > Host mode (fly)`.
В `CombineQueries.prod.fly.cs`: `BaseUrl = https://vq.fly.dev`, `RequireCode = false`, `MemHypers = on`.

`RequireCode = false` поставлен намеренно: при `true` connect набирает кодовое слово побуквенно, по запросу на букву. Это не баг и не новая логика, ветка была всегда — просто в деве флаг `false`, и её не видно.

## Что осталось

1. Перенести словарь и цепочки на хост (см. выше) — от этого зависят все замеры скорости.
2. Токен мира лежит в публичном гите: `appsettings.json`, одиннадцать Designer-файлов миграций, `ContextModelSnapshot.cs`, `AccountEntityConfiguration.cs`. Менять — правка конфига, секрет на fly и миграция для строки Account.
3. Кодовое слово `grach` засвечено на публичной ветке `dev` (`Udon/Archive/CombineQueries.release.cs`). Папка `Udon/Archive/` снята с отслеживания и добавлена в `.gitignore`, но история не чистилась. Само слово уже не действует.
